node_modules/
.expo/
dist/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
app-debug.apk

# Expo CNG generated native dirs (regenerated by prebuild in CI) — root-anchored so
# it does NOT match the nested native/*/android/ Kotlin bridge dirs.
/android/
/ios/
